const setHeaderColor = (color) => {
    document.getElementById("header_color").style.backgroundColor = "orange";

    // header_color.style.backgroundColor = color;

    // var header_color = document.getElementById("header_color");
    // const computedStyle = getComputedStyle(header_color);
    // const backgroundColor = computedStyle.backgroundColor;
    // backgroundColor = color
}